#include <bits/stdc++.h>
using namespace std;

string token(string str, int n){

}

int main(){
    char str[101];

    cin.getline(str, 101);

    int n = strlen(str);

    token(str, n);
    return 0;
}